name: Lightweight Geography Map

on:
  pull_request:
    paths:
      - "backend/leadmap/geography/**"
      - "backend/leadmap/api/geography_routes.py"
      - "backend/tests/**/*geograph*"
      - "frontend/src/**/*Geograph*"
      - "frontend/src/**/*geograph*"
      - "frontend/e2e/lightweight-geography-map.mjs"
      - ".github/workflows/lightweight-geography-map.yml"

permissions:
  contents: read

concurrency:
  group: lightweight-geography-${{ github.ref }}
  cancel-in-progress: true

jobs:
  linux:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Normalize lockfile registry metadata
        run: |
          node - <<'NODE'
          const fs = require('node:fs');
          const path = 'package-lock.json';
          const lock = JSON.parse(fs.readFileSync(path, 'utf8'));
          for (const entry of Object.values(lock.packages ?? {})) {
            if (entry && typeof entry === 'object' && typeof entry.resolved === 'string') {
              if (!entry.resolved.startsWith('https://registry.npmjs.org/')) delete entry.resolved;
            }
          }
          fs.writeFileSync(path, `${JSON.stringify(lock, null, 2)}\n`);
          NODE
      - name: Install
        run: npm ci --no-audit --no-fund --registry=https://registry.npmjs.org
      - name: Install Playwright
        run: |
          npm install --no-save --package-lock=false --ignore-scripts playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Lightweight geography acceptance
        run: timeout 120s node e2e/lightweight-geography-map.mjs
      - name: Upload screenshot
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: lightweight-geography-map-linux
          path: frontend/artifacts/screenshots/lightweight-geography-map.png
          if-no-files-found: error

  windows:
    runs-on: windows-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Normalize lockfile registry metadata
        shell: pwsh
        run: |
          @'
          const fs = require('node:fs');
          const path = 'package-lock.json';
          const lock = JSON.parse(fs.readFileSync(path, 'utf8'));
          for (const entry of Object.values(lock.packages ?? {})) {
            if (entry && typeof entry === 'object' && typeof entry.resolved === 'string') {
              if (!entry.resolved.startsWith('https://registry.npmjs.org/')) delete entry.resolved;
            }
          }
          fs.writeFileSync(path, `${JSON.stringify(lock, null, 2)}\n`);
          '@ | node
      - name: Install
        run: npm ci --no-audit --no-fund --registry=https://registry.npmjs.org
      - name: Install Playwright
        run: |
          npm install --no-save --package-lock=false --ignore-scripts playwright@1.55.0
          npx playwright install chromium
      - name: Lightweight geography acceptance
        run: node e2e/lightweight-geography-map.mjs
      - name: Upload screenshot
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: lightweight-geography-map-windows
          path: frontend/artifacts/screenshots/lightweight-geography-map.png
          if-no-files-found: error
